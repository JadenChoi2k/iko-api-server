server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://[[urlhere!]]:3306/iko?useSSL=false&serverTimezone=Asia/Seoul
    username: admin
    password: iko@admin123!@#
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none #create update none
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect # 버전에 맞추어 변경
